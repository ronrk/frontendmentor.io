import styled from "styled-components";

const StyledCountriesGrid = styled.section`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(20%, 374px));
  justify-content: center;
  gap: 3em;
`;

export default StyledCountriesGrid;
