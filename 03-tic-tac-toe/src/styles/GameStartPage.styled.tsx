import styled from "styled-components";

const GameStartPage = styled.main`
  align-items: center;
  --gap: 0;

  & .logo {
    margin-bottom: 2rem;
  }

  & .btns-container {
    width: 100%;
    --gap: 1rem;
  }
`;

export default GameStartPage;
